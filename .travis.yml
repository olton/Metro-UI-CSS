language: node_js
node_js:
  - 10.8
addons:
  apt:
    packages:
      - libgconf-2-4
before_script:
  - npm install grunt-cli -g
#branches:
#  only:
#    - master
#    - stable
#script:
#  - $(npm bin)/cypress run